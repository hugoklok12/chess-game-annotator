import { type NextPage } from "next";
import Head from "next/head";
import Link from "next/link";

import { trpc } from "../utils/trpc";

const Home: NextPage = () => {
  const hello = trpc.example.hello.useQuery({ text: "from tRPC" });

  return (
    <>
      <Head>
        <title>Create T3 App</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <header className="my-20 mx-auto h-4/6 max-w-7xl rounded bg-white">
        <div className="border-b-grey-300 border border-t p-5">
          <nav className="flex w-full flex-row gap-4">
            <Link href={"/"}>Hues &amp; Cues</Link>
            <Link href={"/game/new"}>New game</Link>
          </nav>
        </div>
        <main className="p-5">
          <h1>hoi</h1>
        </main>
      </header>
    </>
  );
};

export default Home;
